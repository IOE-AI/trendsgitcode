# Public API base (frontend)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Data source provider: github (default) or gitcode
GIT_PROVIDER=github

# Optional tokens (increase rate limits or for authenticated API)
GITHUB_TOKEN=
GITCODE_TOKEN=